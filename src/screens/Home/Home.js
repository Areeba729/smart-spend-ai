import { Text } from 'react-native';
import SafeFlexView from '../../components/SafeFlexView/SafeFlexView';

const Home = () => {
  return (
    <SafeFlexView>
      <Text>Home</Text>
    </SafeFlexView>
  );
};

export default Home;
